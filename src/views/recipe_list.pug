extends layout

block content
    h1= title
    if recipe_list.length
        ul
        each recipe in recipe_list
            li
                a(href=recipe.url) #{recipe.title} - (#{recipe.category})

    else
        p Il n'y a aucune recette.